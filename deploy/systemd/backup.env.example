# Copy to /etc/dokumentverkstad/backup.env; root:dokumentverkstad, mode 640.
# Select and configure a real named rclone remote OUTSIDE the active VPS disk.
# Empty value deliberately prevents activation until configured.
DOKUMENTVERKSTAD_BACKUP_REMOTE=
RCLONE_CONFIG=/etc/dokumentverkstad/rclone/rclone.conf
